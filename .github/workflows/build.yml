name: Build check
on:
  push:
    branches: [main]
  pull_request:
jobs:
  check:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          path: web
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: web/web/package-lock.json
      - name: Install deps
        working-directory: web/web
        run: npm ci
      - name: TypeScript check
        working-directory: web/web
        run: npx tsc --noEmit
